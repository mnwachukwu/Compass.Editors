# Fetched rather than committed. 'npm install' in vscode/ restores them.
node_modules/

# Build output from the test project.
bin/
obj/

# Where a test run writes its .trx, which CI reads to prove nothing skipped.
TestResults/

# Packaged extensions.
*.vsix
